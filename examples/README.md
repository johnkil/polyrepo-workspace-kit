# Examples

This directory contains runnable examples for `wkit`.

- `minimal-workspace/` - a small API/schema rollout workspace with two demo repositories.
- `failure-workspace/` - a scenario evidence demo with pinned ref drift and a failing repo-local check.

The installed CLI also includes self-contained equivalents:

```bash
wkit demo
wkit demo failure
```
